# ============================================================
# Civil Registration Data Quality Engine (CRDQE)
# User Settings
# ============================================================

project:
  name: CRDQE
  version: "1.0"

input:
  folder: "data/input"
  workbook: "Birth_Data.xlsx"
  worksheet: "April"

output:
  folder: "data/output"
  cleaned_folder: "cleaned"
  flags_folder: "flags"
  summaries_folder: "summaries"
  logs_folder: "logs"

processing:
  dataset_detection: auto

logging:
  level: INFO

developer:
  debug: true